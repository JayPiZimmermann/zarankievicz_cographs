# fly.toml app configuration file generated for extremal-cographs on 2026-01-08T20:46:53+01:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'extremal-cographs'
primary_region = 'fra'

[build]
  dockerfile = 'deploy/Dockerfile'
  build-target = ''

[build.args]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '256mb'
  cpus = 1
  memory_mb = 256
